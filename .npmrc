# NPM Configuration for Vercel Deployment
# Resolve peer dependency conflicts

# Force resolution for React version conflicts
legacy-peer-deps=true

# Disable audit warnings during build
audit=false

# Use npm ci in production for faster installs
package-lock=true

# Disable fund messages
fund=false
